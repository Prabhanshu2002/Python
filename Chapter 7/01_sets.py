# set is non-repetitive elements.
# sets are unordered (Elements order doesn't matter)
# sets are unindexed(can't access elements by index)
# there is no way to change items of sets 
# sets can't contain duplicate values



a  = {1,2,3,4,5,1,5,2,3}
print(type(a))
print(a)

